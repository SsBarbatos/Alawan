package com.example.alawan;

import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

public class TestLogin {
    /*
    private FragmentLogin fragmentLogin;

    @Before
    public void setUp() {
        // Initialize LoginManager or mock dependencies
        fragmentLogin = new FragmentLogin();
    }

    @Test
    public void testLoginWithValidCredentials() {
        assertTrue(fragmentLogin.TestLogin("test","test"));
    }

    @Test
    public void testLoginWithInvalidCredentials() {
        assertFalse(fragmentLogin.login("invalidUsername", "invalidPassword"));
    }

    @Test
    public void testLoginWithEmptyUsername() {
        assertFalse(fragmentLogin.login("", "password"));
    }

    @Test
    public void testLoginWithEmptyPassword() {
        assertFalse(fragmentLogin.login("username", ""));
    }
    */
}